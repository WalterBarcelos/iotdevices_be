# Optional: Customize Gradle's memory settings
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Optional: Parallel execution (improves build speed)
org.gradle.parallel=true

# Optional: Enable daemon (improves build speed for subsequent builds)
org.gradle.daemon=true
